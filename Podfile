# Podfile for PostCardIt app
platform :ios, '14.0'

target 'PostCardIt' do
  use_frameworks!
  
  # AWS Core and Authentication
  pod 'AWSCore'
  pod 'AWSCognito'
  pod 'AWSCognitoIdentityProvider'
  
  # AWS S3 for image storage
  pod 'AWSS3'
  
  # Optional - for API Gateway if needed
  # pod 'AWSAPIGateway'
  
  # Optional - for DynamoDB if needed
  # pod 'AWSDynamoDB'
  
  # Optional - for Lambda if needed
  # pod 'AWSLambda'
end
